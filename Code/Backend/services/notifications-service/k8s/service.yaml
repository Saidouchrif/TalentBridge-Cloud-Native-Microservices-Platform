apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  namespace: default
spec:
  selector:
    app: notifications-service
  ports:
    - protocol: TCP
      port: 5003
      targetPort: 5003
  type: ClusterIP
