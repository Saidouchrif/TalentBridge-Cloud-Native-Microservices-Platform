name: ai-document-service

on:
  push:
    paths:
      - "Dev/Microservices/AI-Document-service/**"
      - ".github/workflows/ai-document-service.yml"
  pull_request:
    paths:
      - "Dev/Microservices/AI-Document-service/**"
      - ".github/workflows/ai-document-service.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  test-build-push:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: document_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U test -d document_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    defaults:
      run:
        working-directory: Dev/Microservices/AI-Document-service

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: Dev/Microservices/AI-Document-service/package-lock.json

      - name: Verify tests folder
        run: test -d tests

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        env:
          TEST_DATABASE_URL: postgresql://test:test@localhost:5432/document_test
          DATABASE_URL: postgresql://test:test@localhost:5432/document_test
          JWT_SECRET: ci_jwt_secret
          JWT_ALGORITHM: HS256
          SECRET_KEY: ci_jwt_secret
          ALGORITHM: HS256
          OPENAI_API_KEY: dummy
          GEMINI_API_KEY: ""
        run: npm test -- --silent

      - name: Set image vars
        id: vars
        shell: bash
        run: |
          echo "owner_lc=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"

      - name: Login to GHCR
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ steps.vars.outputs.owner_lc }}/talentbridge-ai-document-service
          tags: |
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: Dev/Microservices/AI-Document-service
          file: Dev/Microservices/AI-Document-service/Dockerfile
          push: ${{ github.event_name == 'push' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

