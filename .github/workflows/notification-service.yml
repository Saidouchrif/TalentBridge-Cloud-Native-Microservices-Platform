name: notification-service

on:
  push:
    paths:
      - "Dev/Microservices/Notification-service/**"
      - ".github/workflows/notification-service.yml"
  pull_request:
    paths:
      - "Dev/Microservices/Notification-service/**"
      - ".github/workflows/notification-service.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  test-build-push:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: notification_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U test -d notification_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    defaults:
      run:
        working-directory: Dev/Microservices/Notification-service

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: Dev/Microservices/Notification-service/package-lock.json

      - name: Verify tests folder
        run: test -d tests

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        env:
          TEST_DATABASE_URL: postgresql://test:test@localhost:5432/notification_test
          DATABASE_URL: postgresql://test:test@localhost:5432/notification_test
          JWT_SECRET: ci_jwt_secret
          JWT_ALGORITHM: HS256
          SECRET_KEY: ci_jwt_secret
          ALGORITHM: HS256
          NOTIFICATION_INTERNAL_TOKEN: ci_notify_token
          SERVICE_INTERNAL_TOKEN: ci_notify_token
          AUTH_SERVICE_URL: http://127.0.0.1:9
          AUTH_SERVICE_ADMIN_TOKEN: ci_notify_token
        run: npm test -- --silent

      - name: Set image vars
        id: vars
        shell: bash
        run: |
          echo "owner_lc=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"

      - name: Login to GHCR
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ steps.vars.outputs.owner_lc }}/talentbridge-notification-service
          tags: |
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: Dev/Microservices/Notification-service
          file: Dev/Microservices/Notification-service/Dockerfile
          push: ${{ github.event_name == 'push' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

